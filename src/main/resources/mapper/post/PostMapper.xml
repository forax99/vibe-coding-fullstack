<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.vibeapp.post.PostRepository">

    <!-- ResultMap: post 테이블 컬럼 → Post 객체 매핑 -->
    <resultMap id="postResultMap" type="Post">
        <id     property="no"        column="no"/>
        <result property="title"     column="title"/>
        <result property="content"   column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="views"     column="views"/>
    </resultMap>

    <!-- 전체 목록 조회 (최신순) -->
    <select id="findAll" resultMap="postResultMap">
        SELECT no, title, content, created_at, updated_at, views
        FROM post
        ORDER BY no DESC
    </select>

    <!-- 페이징 목록 조회 -->
    <select id="findPaged" resultMap="postResultMap">
        SELECT no, title, content, created_at, updated_at, views
        FROM post
        ORDER BY no DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 총 게시글 수 -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM post
    </select>

    <!-- 단건 조회 -->
    <select id="findByNo" resultMap="postResultMap">
        SELECT no, title, content, created_at, updated_at, views
        FROM post
        WHERE no = #{no}
    </select>

    <!-- 저장 (no가 없으면 INSERT, 있으면 UPDATE) -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO post (title, content, created_at, updated_at, views)
        VALUES (#{title}, #{content}, #{createdAt}, #{updatedAt}, #{views})
    </insert>

    <!-- 수정 -->
    <update id="update">
        UPDATE post
        SET title      = #{title},
            content    = #{content},
            updated_at = #{updatedAt},
            views      = #{views}
        WHERE no = #{no}
    </update>

    <!-- 삭제 -->
    <delete id="deleteByNo">
        DELETE FROM post WHERE no = #{no}
    </delete>

</mapper>
